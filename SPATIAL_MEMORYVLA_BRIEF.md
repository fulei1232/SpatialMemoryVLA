# SpatialMemoryVLA：创新、贡献与实验设计

## 目标

在不改变 MemoryVLA 基础动作学习设置的前提下，增强策略对物体相对位置、空间关系和跨时刻状态的表征能力，从而提升空间操控与长程任务的可靠性。

## 创新点

1. **几何表征对齐。** 使用冻结的 VGGT 作为仅训练期的空间教师；将 VLA 第 24 层视觉 token 从 `[B,256,4096]` 投影至 `[B,256,2048]`，以余弦损失对齐 VGGT 的几何特征。总损失为 `L_action + 0.5 × L_spatial`。
2. **几何对齐的时序记忆。** 将对齐后的空间 token 压缩为感知记忆，并接入 MemoryVLA 的工作记忆、episode memory、检索与门控融合链路；空间表示不仅受监督，也参与后续动作决策。
3. **训练—推理解耦。** VGGT 始终冻结、位于 FSDP 与策略 checkpoint 之外；推理时仅保留已学得的投影器、压缩器和记忆模块，不增加教师模型依赖。

## 贡献点

- 提出可插拔的 Geometry-Aligned Temporal Memory：空间对齐和时序记忆可独立启停，便于定位各模块收益。
- 建立从视觉 token、空间教师特征到记忆 token 的显式张量契约与有限性检查，降低多卡训练中的静默错配风险。
- 统一数据、初始化、优化器、batch size、训练步数和评估协议，构建公平的 A/B/C 因果消融。

## 实验设计

### 对照组

| 组别 | 空间对齐 | 空间记忆 | 回答的问题 |
| --- | --- | --- | --- |
| A：MemoryVLA baseline | 否 | 否 | 基线能力 |
| B：Spatial Forcing | 是 | 否 | 仅空间辅助监督是否有效 |
| C：SpatialMemoryVLA | 是 | 是 | 记忆消费空间表征的额外收益 |

三组共享 LIBERO-Spatial `libero_spatial_no_noops` 数据、相同预训练初始化、8 GPU、global batch 256、学习率 `2e-5`、10,000 steps、每 2,000 steps 保存一次。唯一的功能性变量是空间对齐与空间记忆开关。

### 评估与判据

- 训练期：记录 `Action Loss`、`Spatial Loss`、空间项相对权重、Grad Norm、总损失和 checkpoint 可恢复性。
- 策略期：在 LIBERO-Spatial 官方任务上以相同 checkpoint 规则、seed、CFG、action chunk 和 episode 数比较成功率；随后在长程/多阶段任务上验证时序收益。
- 关键比较：`B − A` 衡量空间对齐贡献，`C − B` 衡量空间记忆贡献，`C − A` 衡量完整方法收益。

## 预期效果

- B 相比 A：空间关系判别更稳定，尤其是目标物体、容器和相对位置约束。
- C 相比 B：在遮挡、视角变化和多步状态演化下，保持更一致的空间上下文，提升长程任务完成率。
- 完整方法应在不牺牲动作优化稳定性的前提下，提高 LIBERO 空间任务成功率；若收益仅出现在 B，则说明监督有效但记忆利用仍需改进。

## 结论边界

当前训练损失与稳定性只能验证实现和优化链路；方法效果以 A/B/C 的同协议 rollout 成功率为最终依据，未完成的对比评估不得表述为性能结论。
